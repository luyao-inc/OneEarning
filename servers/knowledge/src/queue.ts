/**
 * Phase 2: drain `jobs` table with a single worker for heavy extractors (ASR/OCR).
 * Phase 1: unused; reserved for API compatibility.
 */
export async function startJobWorker(): Promise<void> {
  /* no-op */
}
